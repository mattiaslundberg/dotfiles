snippet define
    //>>pure-amd
    define([
        "${1}"
    ], function (
        ${2}
    ) {
        ${3}
    });

snippet declare
    //>>pure-amd
    define([
        "dojo/_base/declare",
        "${1}"
    ], function (
        declare,
        ${2}
    ) {
        return declare("${3}", [${4}], {
            ${5}
        });
    });

snippet require
    require(["${1}"], function(${2}) {
        ${3}
    });
